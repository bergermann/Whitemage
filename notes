# switch shell to superuser:
su superuser

# load env variables as bash variables (not yet tested)
source envvariables

# influx, grafana setup guide:
https://constellation.pages.desy.de/operator_guide/howtos/setup_influxdb_grafana.html

# added uh2madmx to group docker
# start docker with
cd slow_control/docker
docker compose up -d

#stop docker with
docker compose down

# influx, grafana account names: admin
# pw same as uh2madmx

